import { Route } from '@angular/router';
import { ProfilePageComponent } from './profile-page.component';

export const profilePageRoutes: Route[] = [
  { path: '', component: ProfilePageComponent },
];
